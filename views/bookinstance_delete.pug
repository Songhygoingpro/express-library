extends layout

block content
  h1 Delete Book Instance
  p Are you sure you want to delete this book instance?

  form( method="POST")
    input(type="hidden" name="bookinstanceid" value=bookInstance._id)

    button(type="submit") Delete
    a(href="/catalog/bookinstances") Cancel
